# Implementation Plan: 자율 판단 코어 (Autonomous Core)

**Status**: ✅ Completed
**Plan ID**: PLAN_006
**Started**: 2026-01-04
**Last Updated**: 2026-01-04
**Estimated Completion**: 3-5일
**Actual Completion**: 2026-01-04 (약 2시간)

---

**⚠️ CRITICAL INSTRUCTIONS**: After completing each phase:
1. ✅ Check off completed task checkboxes
2. 🧪 Run all quality gate validation commands
3. ⚠️ Verify ALL quality gate items pass
4. 📅 Update "Last Updated" date above
5. 📝 Document learnings in Notes section
6. ➡️ Only then proceed to next phase

⛔ **DO NOT skip quality gates or proceed with failing checks**

---

## 📋 Overview

### Feature Description

패니저 v2.0의 핵심인 **자율 판단 코어**를 구현합니다.
LangGraph 기반의 **Observe-Think-Act-Reflect** 루프를 통해 LLM이 스스로 상황을 판단하고 행동합니다.

```
┌──────────────────────────────────────────────────────────────────────────┐
│                        자율 판단 루프 (LangGraph)                         │
│                                                                          │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│   │  Observe │───▶│  Think   │───▶│   Act    │───▶│ Reflect  │         │
│   │  (관찰)  │    │  (판단)  │    │  (행동)  │    │  (반성)  │         │
│   └──────────┘    └────┬─────┘    └──────────┘    └────┬─────┘         │
│                        │                               │                │
│                        │ decision="wait"               │ lesson         │
│                        │      │                        │                │
│                        └──────┼────────────────────────┘                │
│                               ▼                                         │
│                         ┌─────────┐                                     │
│                         │  Wait   │────▶ (다음 루프)                    │
│                         └─────────┘                                     │
└──────────────────────────────────────────────────────────────────────────┘
```

### Success Criteria

- [x] LangGraph 기반 StateGraph 구현
- [x] Observe 노드: 현재 상태(시간, 날씨, 일정) 수집
- [x] Think 노드: LLM 기반 행동 필요성 판단
- [x] Act 노드: 알림/리마인더 실행
- [x] Reflect 노드: 행동 결과 분석 및 교훈 추출
- [x] AutonomousRunner 클래스 구현
- [x] 모든 테스트 통과 (223 passed)

> **Note**: 유동적 실행 주기는 PLAN_007 메모리 시스템에서 구현 예정

### Related Requirements

| 요구사항 ID | 명칭 | 우선순위 |
|-------------|------|----------|
| FR-101 | 상황 관찰 (Observe) | Critical |
| FR-102 | LLM 기반 판단 (Think) | Critical |
| FR-103 | 행동 실행 (Act) | Critical |
| FR-104 | 행동 결과 반성 (Reflect) | Critical |

---

## 🏗️ Architecture Decisions

### 기술 스택

| 구성요소 | 선택 | 이유 |
|----------|------|------|
| 그래프 프레임워크 | LangGraph | State 기반 에이전트 구현에 최적 |
| LLM | Groq (llama-3.1-70b) | 무료, 빠른 응답 |
| 상태 관리 | TypedDict | 타입 안전성, IDE 지원 |
| 스케줄러 | APScheduler | 기존 코드 재사용 |

### 디렉토리 구조

```
src/
├── core/
│   └── autonomous/           # 🆕 자율 판단 시스템
│       ├── __init__.py
│       ├── graph.py          # LangGraph StateGraph 정의
│       ├── state.py          # AgentState TypedDict
│       ├── nodes/
│       │   ├── __init__.py
│       │   ├── observe.py    # Observe 노드
│       │   ├── think.py      # Think 노드
│       │   ├── act.py        # Act 노드
│       │   └── reflect.py    # Reflect 노드
│       └── prompts/
│           ├── __init__.py
│           ├── think.py      # THINK_PROMPT
│           └── reflect.py    # REFLECT_PROMPT
│
└── main.py                   # 자율 루프 스케줄링 추가
```

### 의사결정 흐름

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Think 노드 의사결정                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   1. confidence < 0.7?  ─── YES ──▶ decision = "wait"                      │
│         │                                                                   │
│        NO                                                                   │
│         ▼                                                                   │
│   2. 2시간 내 동일 정보 전송?  ─── YES ──▶ decision = "wait"               │
│         │                                                                   │
│        NO                                                                   │
│         ▼                                                                   │
│   3. 방해 금지 시간(23~07)?  ─── YES ──▶ decision = "wait" (긴급 제외)     │
│         │                                                                   │
│        NO                                                                   │
│         ▼                                                                   │
│   4. 일일 알림 7회 초과?  ─── YES ──▶ decision = "wait"                    │
│         │                                                                   │
│        NO                                                                   │
│         ▼                                                                   │
│   decision = "act"                                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📦 Dependencies

### 선행 조건
- PLAN_009 (Legacy Cleanup): ✅ 완료됨
- PLAN_001 (Core): ✅ 완료됨 (도구, LLM 클라이언트 존재)

### 외부 의존성
```bash
# 추가 필요한 패키지
poetry add langgraph
```

### 후속 작업
- PLAN_007 (Memory System): 이 작업 완료 후 진행
- PLAN_008 (Prompt Engineering): 이 작업 완료 후 진행

---

## 🔬 Test Strategy

### 테스트 접근법
- **Unit Test**: 각 노드 개별 테스트
- **Integration Test**: 전체 그래프 흐름 테스트
- **Mock**: LLM API, 외부 서비스 모킹

### 테스트 시나리오

| 시나리오 | 입력 | 기대 결과 |
|----------|------|----------|
| 비 예보 + 외출 일정 | weather.needs_umbrella=True, upcoming_schedule 존재 | decision="act", 우산 알림 |
| 일정 없음, 날씨 좋음 | weather.needs_umbrella=False, schedules=[] | decision="wait" |
| 방해 금지 시간 | current_time=02:00 | decision="wait" |
| 알림 7회 초과 | today_notification_count=7 | decision="wait" |
| 부정적 반응 | user_reaction="그만해" | lesson 생성 |

### 커버리지 목표
- 전체: 80% 이상
- nodes/: 90% 이상 (핵심 로직)

---

## 📝 Implementation Phases

### Phase 1: AgentState 및 기본 구조 설정 (1-2시간) ✅

**목표**: 자율 판단 시스템의 기본 미대 구성

**Tasks**:
- [x] `poetry add langgraph` 설치
- [x] `src/core/autonomous/` 디렉토리 생성
- [x] `state.py`: AgentState TypedDict 정의
- [x] `graph.py`: StateGraph 스켈레톤 생성
- [x] 노드 스켈레톤 생성 (observe, think, act, reflect)
- [x] 프롬프트 스켈레톤 생성 (THINK_PROMPT, REFLECT_PROMPT)

**Quality Gate**:
- [x] 린트 통과 (`poetry run ruff check src/`)
- [x] Import 성공 (`python -c "from src.core.autonomous.state import AgentState"`)
- [x] Graph 생성 성공 (`from src.core.autonomous.graph import create_autonomous_agent`)
- [x] 기존 테스트 통과 (150 passed)

---

### Phase 2: Observe 노드 구현 (2-3시간) ✅

**목표**: 현재 상태 정보를 수집하는 Observe 노드 구현

**Tasks**:
- [x] `nodes/observe.py` 구현
- [x] 현재 시간 정보 수집 (time_period, is_quiet_hours)
- [x] 날씨 정보 수집 (CachedWeatherService 연동)
- [x] 일정 정보 수집 (CalendarService 연동)
- [x] 최근 알림 이력 조회 (state에서 조회)
- [x] 테스트 작성 및 통과 (13 tests)

**Quality Gate**:
- [x] 린트 통과
- [x] 테스트 통과 (`poetry run pytest tests/unit/core/autonomous/test_observe.py` - 13 passed)
- [x] 전체 테스트 통과 (163 passed)

---

### Phase 3: Think 노드 구현 (3-4시간) ✅

**목표**: LLM 기반 판단 로직 구현

**Tasks**:
- [x] `prompts/think.py`: THINK_PROMPT 템플릿 작성
- [x] `nodes/think.py` 구현
- [x] LLM 호출 및 JSON 파싱
- [x] 비즈니스 규칙 적용 (confidence, 방해 금지, 알림 한도)
- [x] 테스트 작성 및 통과 (14 tests)

**Quality Gate**:
- [x] 린트 통과
- [x] 테스트 통과 (`poetry run pytest tests/unit/core/autonomous/test_think.py` - 14 passed)
- [x] JSON 파싱 에러 핸들링 완료
- [x] 전체 테스트 통과 (177 passed)

---

### Phase 4: Act 노드 구현 (2-3시간) ✅

**목표**: 판단 결과에 따른 행동 실행

**Tasks**:
- [x] `nodes/act.py` 구현
- [x] decision="act" 시 알림 전송
- [x] decision="wait" 시 스킵
- [x] 알림 이력 기록 (메모리 기반)
- [x] 테스트 작성 및 통과 (13 tests)

**Quality Gate**:
- [x] 린트 통과
- [x] 테스트 통과 (`poetry run pytest tests/unit/core/autonomous/test_act.py` - 13 passed)
- [x] 알림 전송 성공 확인 (Mock)
- [x] 전체 테스트 통과 (190 passed)

---

### Phase 5: Reflect 노드 구현 (2-3시간) ✅

**목표**: 행동 결과 분석 및 교훈 추출

**Tasks**:
- [x] `prompts/reflect.py`: REFLECT_PROMPT 템플릿 (Phase 1에서 작성됨)
- [x] `nodes/reflect.py` 구현
- [x] 사용자 반응 분석 (긍정/부정/중립 키워드 기반)
- [x] 교훈 추출 및 저장 (부정적 반응 시 LLM으로 추출)
- [x] 테스트 작성 및 통과 (20 tests)

**Quality Gate**:
- [x] 린트 통과
- [x] 테스트 통과 (`poetry run pytest tests/unit/core/autonomous/test_reflect.py` - 20 passed)
- [x] 교훈 저장 로직 검증 (메모리 기반, 최대 50개)
- [x] 전체 테스트 통과 (210 passed)

---

### Phase 6: 그래프 통합 (2-3시간) ✅

**목표**: 모든 노드를 연결하고 AutonomousRunner 구현

**Tasks**:
- [x] `graph.py`: StateGraph 완성
- [x] 노드 연결 (observe → think → act → reflect)
- [x] 조건부 엣지 (decision에 따른 분기)
- [x] AutonomousRunner 클래스 구현
- [x] 테스트 작성 및 통과 (13 tests)

**Quality Gate**:
- [x] 린트 통과
- [x] 전체 테스트 통과 (`poetry run pytest tests/` - 223 passed)
- [x] 그래프 생성 및 실행 성공

**Scope 조정**:
> 고정 5분 주기 스케줄링은 제외.
> 유동적 실행 주기(사용자 데이터 기반)는 PLAN_007 메모리 시스템에서 구현 예정.

---

## 📊 Risk Assessment

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|------------|
| LangGraph 학습 곡선 | Medium | Medium | 공식 문서 및 예제 참조 |
| LLM 응답 불안정 | Low | High | JSON 파싱 에러 핸들링, 재시도 |
| Groq Rate Limit | Medium | High | 5분 간격으로 호출 최소화 |
| 상태 관리 복잡성 | Medium | Medium | TypedDict로 타입 안전성 확보 |

---

## 🔄 Rollback Strategy

각 Phase별로 Git commit 생성:

```bash
# Phase별 커밋
git commit -m "feat(PLAN_006): Phase 1 - AgentState 기본 구조"
git commit -m "feat(PLAN_006): Phase 2 - Observe 노드"
...

# 문제 발생 시 롤백
git revert <commit-hash>
```

---

## 📈 Progress Tracking

```
Phase 1: 기본 구조        █████ 100% ✅
Phase 2: Observe 노드     █████ 100% ✅
Phase 3: Think 노드       █████ 100% ✅
Phase 4: Act 노드         █████ 100% ✅
Phase 5: Reflect 노드     █████ 100% ✅
Phase 6: 그래프 통합      █████ 100% ✅
─────────────────────────────────
Total:                    █████ 100% ✅
```

---

## 📝 Notes & Learnings

- Python 버전 제약을 `>=3.11,<4.0`으로 변경해야 LangGraph 설치 가능
- LangGraph 1.0.5 설치 완료
- 스켈레톤 구조로 Phase 2~5의 기반 마련
- Observe 노드: 동기/비동기 두 가지 버전 제공
- 서비스 에러 핸들링으로 안정성 확보
- Think 노드: 비즈니스 규칙 우선 적용으로 LLM 호출 최소화
- JSON 파싱 실패 시 기본값(wait) 반환으로 안정성 확보
- Act 노드: 알림 이력을 메모리에 저장 (최대 100개)
- Reflect 노드: 긍정 키워드 먼저 체크 (순서 중요)
- AutonomousRunner: 비동기 노드 오케스트레이션
- 총 223개 테스트 통과
- **Scope 조정**: 고정 주기 스케줄링 제외, 유동적 주기는 PLAN_007에서 구현

---

## 🔗 Related Documents

- [PLAN_master.md](./PLAN_master.md) - 통합 계획서
- [PLAN_legacy_cleanup.md](./PLAN_legacy_cleanup.md) - 레거시 정리 (완료)
- [SRS_autonomous_panager.md](../SRS_autonomous_panager.md) - 요구사항 정의서
- [PLAN_memory_system.md](./PLAN_memory_system.md) - 메모리 시스템 (후속)
- [PLAN_prompt_engineering.md](./PLAN_prompt_engineering.md) - 프롬프트 엔지니어링 (후속)
